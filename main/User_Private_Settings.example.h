#pragma once

/*
 * Copy this file to:
 *
 *     main/User_Private_Settings.h
 *
 * Then enter the real Wi-Fi credentials.
 *
 * User_Private_Settings.h is ignored by Git and must never be
 * committed because it contains private credentials.
 */

const char USER_WIFI_SSID[] = "YOUR_WIFI_SSID";
const char USER_WIFI_PASS[] = "YOUR_WIFI_PASSWORD";